---
type: llm
weight: 1
---

Pass only if the spec maps the column keys `c1` and `c2`, not the labels Month and Revenue.

Fail if the mapping uses `month`, `revenue`, `Month`, or `Revenue` as the column names.
