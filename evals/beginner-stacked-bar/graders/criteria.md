---
type: llm
weight: 1
---

Pass only if the bar layer uses `position: 'stack'` and a color mapping distinguishes Alpha and Beta.

Fail if the bars are only side by side, or if there is no stack position.
