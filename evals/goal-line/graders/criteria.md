---
type: llm
weight: 1
---

Pass only if the answer adds the goal with `geom.rule()` on the bar chart, and the rule value is 100.

Fail if the goal is drawn with a plugin, a pie geom, or an annotation instead of `geom.rule()`.
