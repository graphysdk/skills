---
type: llm
weight: 1
---

Pass only if the bars are grouped side by side, using a color mapping and either the default bar position or `position: 'dodge'`.

Fail if the bars are stacked with `position: 'stack'`.
