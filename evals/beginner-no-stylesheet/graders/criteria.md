---
type: llm
weight: 1
---

Pass only if the install is `@graphysdk/react` and the component imports the chart from that package with no separate Graphy stylesheet import.

Fail if the answer tells the user to import a `.css` file from the Graphy package.
