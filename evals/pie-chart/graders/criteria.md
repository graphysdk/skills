---
type: llm
weight: 1
---

Pass only if the pie chart is a bar geom on polar coordinates: `geom.bar` together with `coord.polar`. A donut may add an inner radius.

Fail if the answer uses a geom named pie or donut, or if it does not use polar coordinates.
