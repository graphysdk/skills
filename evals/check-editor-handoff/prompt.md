---
max_turns: 8
allowed_tools: [Read, Glob, Grep, Skill]
---

We already have a bar chart built with the Graphy SDK in React, using the standard Graphy React package from the normal install. Now I want people to be able to edit that chart in the page, and undo their changes if they make a mistake. How do I set that up? Show the code.
