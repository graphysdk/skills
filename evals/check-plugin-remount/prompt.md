---
max_turns: 8
allowed_tools: [Read, Glob, Grep, Skill]
---

We use a couple of custom Graphy plugins in our React app. A chart is already on screen, and when the user flips a toggle I want it to switch to a different set of plugins. What's the right way to swap them out on a chart that's already mounted? Show the code.
