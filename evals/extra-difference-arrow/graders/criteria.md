---
type: llm
weight: 1
---

Pass only if the spec adds `annotation.differenceArrow` with `start` and `end` anchored on the 2023 and 2025 bars.

Fail if the change appears only in a title, subtitle, or text annotation. Also fail if it is drawn with a plain `annotation.arrow` instead of `annotation.differenceArrow`.
